@* The TelerikDialogHelper component allows you to use the DialogFactory in interactive Razor components,
    which are NOT children of the TelerikRootComponent or the TelerikContainer.

    The TelerikDialogHelper MUST be a child of TelerikRootComponent or TelerikContainer to work.
*@

@code {
    [CascadingParameter]
    public DialogFactory? Dialogs { get; set; }

    public async Task AlertAsync(string message)
    {
        if (Dialogs != null)
        {
            await Dialogs.AlertAsync(message);
        }
    }
}
